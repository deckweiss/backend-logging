stages:
  - build
  - test
  - publish

# build
build_package:
  image: gradle:jdk17
  stage: build
  script:
    - gradle assemble
  cache:
    paths:
      - .gradle/

# test
test_package:
  image: gradle:jdk17
  stage: test
  script:
    - gradle test
  allow_failure: false
  cache:
    paths:
      - .gradle/
  needs:
    - build_package

# publish
publish_package:
  image: gradle:jdk17
  stage: publish
  script:
    - gradle publish
  cache:
    paths:
      - .gradle/
  only:
    refs:
      - master
  needs:
    - test_package